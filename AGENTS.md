# cc-harness · Codex 협업 지침

## 범위와 읽기 원칙

- 이 저장소는 제품 앱이 아니라 독립형 Codex 하네스다. 실행 스킬, 방법론, 구조도, Vitest 이식 회귀·타입 검사를 유지하며 원격 CI는 없다.
- 실제 파일·적용 범위의 지침을 먼저 확인하고 기존 구조를 재사용한다. 미사용 원본·호환 파일·임시 자료·비밀 값·`node_modules`·원본 PDF를 Git에 넣지 않는다.
- 현재 단계의 `SKILL.md`와 그 스킬이 지정한 문서 절만 읽는다. 절 선택은 **지정 제목부터 다음 같은 수준 또는 더 높은 수준 제목 직전까지**다. Markdown 앵커가 읽기 범위를 자동 제한한다고 가정하지 않는다.
- 파일을 바꿀 때는 먼저 source revision·dirty 상태와 변경 소유를 확인한다. lifecycle/RESUME 또는 증거 재사용 때만 checkpoint와 승인·증거 식별 정보를 대조하고, 관련 contract/AC/code/test/command/environment가 같은 증거의 실제 결과를 확인한다. 긴 로그는 파일로 남기며 충돌·UNKNOWN이면 관련 범위만 넓힌다.
- 의미 단계 완료 뒤 다음 작업 묶음을 고르거나 실제 context pressure가 관찰된 때에만 `docs/harness/lifecycle.md`의 `세션 checkpoint와 handoff` 절을 읽고, 이미 읽은 동일 계약과 상세 schema는 반복 적재하지 않는다.
- 상세 문서나 registry를 처음부터 전수 읽지 않는다. registry는 등록·이식·검사를 실제 수행할 때만 사용한다. 요구사항·아키텍처·범위·이슈·시나리오 승인 근거와 RESUME의 기존 유효 증거는 보존한다.

## 실행 계약

- 대화형 실행, 자동 실행, 하네스 유지보수 모두 [필수 오케스트레이션 정책](docs/methods/delivery-automation.md#필수-오케스트레이션-정책)을 적용한다. 메인은 상태·승인·라우팅·증거 통합·사용자 대화와 진행 안내를 맡고, 실제 탐색·작성/수정·테스트는 최소 입력의 새 작업 컨텍스트에 있는 작업자에게 맡긴다. 작은 구현도 예외가 없다.
- 작업자는 받은 유한 범위를 직접 수행하고 재귀 위임하지 않는다. 메인은 구현자와 분리된 검증자를 배정한다. 도구가 없으면 정확한 장애와 재개 조건을 알리고 메인이 대신 구현하거나 독립 검증을 가장하지 않는다.
- 승인 해석은 `docs/harness/lifecycle.md`의 `## 5. 승인과 외부 작업`을 따른다. 명확한 추천 결과에 대한 “승인하고 구현”은 그 결과와 가역적 로컬 구현·검증의 승인으로 기록하며, 통상적인 G2–G5 선택에 게이트 이름·추가 양식·재승인을 요구하지 않는다. 사용자 원문·승인 결과·실행 범위와 AI 선택·이유를 구분하고 게이트 산출물과 G4·G5 독립 검토는 갖춘다.
- 승인된 범위의 가역 수정·검증·실패 복구는 끝내되 구체적인 승인 제한, lifecycle 게이트와 객관 STOP은 유지한다. 별도 권한 없이 Git 초기화·브랜치·커밋·푸시·PR·병합·배포·유료 호출·파괴적 작업·전역 설정 변경이나 다른 외부 쓰기를 하지 않는다.
- 기본 변경 예산은 새 추상화 1개 이하·새 의존성 0개·변경 파일 4개 이하다. 초과가 필요하면 이유와 최소 범위를 먼저 알린다.
- 전 사이클은 `harness-cycle`, 기획은 `feature-planner`, 이슈는 `tdd-loop`로 연결한다. `tdd-auto-loop`는 사용자가 유한 범위와 상한을 명시적으로 위임했을 때만 쓴다. 해당 스킬의 STOP·누적 Green/diagnostic 예산·metadata/evidence/RESUME 계약을 유지한다.

## 검사와 보고

- 설치 `npm ci`, 코드 `npm run lint`, 서식 `npm run format:check`, 수정 `npm run format`, 최종 통합 `npm run check`를 사용한다. `npm run check`는 ESLint·구조도 디자인 정적 검사·하네스·Prettier·이식 회귀·타입 검사이며 앱 빌드·브라우저·Windows 실기능·원격 CI 증거가 아니다.
- 관련 검사 1회 → 실패 원인 수정과 해당 검사 재실행 → 마지막 전체 검사 1회를 기본으로 한다. 새 변경·실패·필수 검사 없이 반복하지 않는다. 훅을 우회하거나 오류를 숨기지 않는다.
- 장시간 작업은 진행 상황을 알리고 완료·미구현·미검증을 구분한다. 실제 명령, 결과, 남은 위험과 재개 조건을 보고한다.

## 조건부 지침

- 요구사항 인터뷰는 `feature-planner`와 `docs/methods/requirements-interview.md`의 현재 절을 따른다. 앱이 없으면 없다고 밝힌다. 목적·가치·중대한 범위나 위험의 공백은 한 번에 질문 하나로 확인하고, 구현 승인 범위의 낮은 위험의 세부는 기본값·이유로 기록한다. 필수 정책을 미결로 둔 채 G1을 통과시키지 않는다.
- UI 변경은 `.agents/skills/design-system/SKILL.md`와 대상 `design.md`를 읽고 `npm run design:check`를 실행한다. 루트 구조도 검사는 제품 디자인 승인이나 화면 검증이 아니다.
- 커밋 요청 시 `type(scope): 한국어 설명`을 사용한다(scope 생략 가능; type은 commitlint 규칙). 도구 통과가 한국어·내용 일치를 보증하지 않는다.
- 구조도는 명시적으로 요청될 때 `.agents/skills/mermaid-diagram/SKILL.md`를 따른다. 문서 변경과 브라우저 렌더링 검증을 구분하며 상시 감시라고 주장하지 않는다.
